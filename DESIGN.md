# DESIGN.md

Design System ของ **BuildTrack — ระบบบริหารโครงการก่อสร้าง**

เอกสารนี้เป็นมาตรฐานการออกแบบที่ทุกหน้าจอต้องยึด ทุกข้อในเอกสารนี้สืบมาจากข้อกำหนดจริงใน [[docs/01-requirements/01-spec/index|01-spec]] — ไม่มีข้อใดที่เป็นรสนิยมล้วน ถ้าข้อกำหนดเปลี่ยน เอกสารนี้ต้องเปลี่ยนตาม

> **ขอบเขต**: เอกสารนี้กำหนด *ภาษาการออกแบบ* ไม่ใช่หน้าจอของฟีเจอร์ใด การออกแบบหน้าจอจริงอยู่ใน `docs/02-design/01-prototypes/` และทำได้เมื่อ spec ของฟีเจอร์นั้นผ่านการอนุมัติแล้ว

---

## 1. Brand Identity & CI

### 1.1 ตัวตนของผลิตภัณฑ์

BuildTrack ไม่ใช่แอปสำหรับผู้บริโภค แต่เป็น**เครื่องมือทำงานที่คนใช้ทุกวันในสภาพแวดล้อมที่ไม่เอื้อ** — กลางแดด มือเปื้อน สัญญาณไม่เสถียร และตัวเลขที่แสดงมีผลต่อเงินหลักสิบล้าน

| คุณลักษณะ | หมายความว่าอย่างไรในการออกแบบ |
| --- | --- |
| **น่าเชื่อถือ** | ตัวเลขต้องอ่านผิดไม่ได้ ไม่มีการตกแต่งที่แข่งกับข้อมูล |
| **ตรงไปตรงมา** | บอกสถานะจริงแม้เป็นข่าวร้าย ไม่ทำให้ตัวเลขแย่ดูดีขึ้นด้วยการออกแบบ |
| **ทนสภาพหน้างาน** | ใช้ได้ด้วยมือสวมถุงมือกลางแดด ไม่ใช่แค่บนโต๊ะทำงาน |
| **ไม่ต้องเรียนก่อนใช้** | ผู้บริหารที่ไม่รู้ศัพท์ EVM ต้องเข้าใจได้ทันที (NFR-002-03) |

**สิ่งที่จงใจไม่เป็น**: ไม่ใช้ภาพสวยงามของอาคาร ไม่ใช้ gradient หรือ glassmorphism ไม่มี animation ที่หน่วงการแสดงข้อมูล — ทุกอย่างที่ไม่ได้ช่วยให้อ่านตัวเลขเร็วขึ้นคือสิ่งรบกวน

### 1.2 สีหลักของแบรนด์

สีแบรนด์ต้อง**ไม่ชนกับสีสถานะ** เพราะเขียว/เหลือง/แดงถูกจองไว้แล้วโดย BR-001-05 ของ SPEC-001 — สีแบรนด์จึงเป็นน้ำเงินเข้มอมเทา ซึ่งอ่านเป็นงานวิศวกรรมและโครงสร้างพื้นฐาน ไม่ใช่แอปทั่วไป

```
Primary  #1B3A5C   ใช้กับ header, ปุ่มหลัก, ลิงก์
```

### 1.3 การใช้โลโก้

- ระยะปลอดภัยรอบโลโก้ = ความสูงตัวอักษร B ของคำว่า BuildTrack
- ขนาดเล็กสุด 24px สำหรับหน้าจอ / 16mm สำหรับงานพิมพ์
- ห้ามยืด บิด เปลี่ยนสี ใส่เงา หรือวางบนภาพถ่ายที่ไม่มีแผ่นรองทึบ

### 1.4 น้ำเสียง (Tone of Voice)

ภาษาไทยทั้งหมด สุภาพแต่ไม่อ้อมค้อม — บอกสิ่งที่เกิดขึ้นและสิ่งที่ต้องทำต่อ

| สถานการณ์ | ✅ เขียนแบบนี้ | ❌ ไม่ใช่แบบนี้ |
| --- | --- | --- |
| ต้นทุนเกิน | "ต้นทุนที่จ่ายจริงสูงกว่ามูลค่างานที่ทำได้ 15%" | "CPI = 0.85" |
| ไม่มีข้อมูล | "ยังไม่ได้ตั้ง baseline จึงคำนวณ SPI ไม่ได้" | "N/A" หรือช่องว่าง |
| ทำไม่สำเร็จ | "บันทึกไม่สำเร็จ ข้อมูลถูกเก็บไว้ในเครื่องแล้ว จะส่งอัตโนมัติเมื่อมีสัญญาณ" | "เกิดข้อผิดพลาด" |
| ยืนยันการทำ | "ตีกลับงวดที่ 3 — โปรดระบุเหตุผลให้ผู้รับเหมา" | "คุณแน่ใจหรือไม่?" |

---

## 2. Design Tokens

Token คือชื่อของค่า ห้ามใส่ค่าดิบลงในหน้าจอโดยตรง — ถ้าค่าที่ต้องการไม่มี token แปลว่ายังไม่มีใครตัดสินใจเรื่องนั้น ให้ถามก่อนเพิ่ม

### 2.1 Colors

**สีแบรนด์**

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--color-primary-900` | `#0F2438` | ข้อความบนพื้นสว่าง, header เข้ม |
| `--color-primary-700` | `#1B3A5C` | ปุ่มหลัก, สีแบรนด์ |
| `--color-primary-500` | `#2C5D8F` | สถานะ hover, ลิงก์ |
| `--color-primary-300` | `#7FA6CC` | เส้นขอบเน้น, สถานะ disabled ของปุ่มหลัก |
| `--color-primary-100` | `#DCE8F4` | พื้นหลังส่วนที่ถูกเลือก |

**สีสถานะ — ผูกกับ BR-001-05 โดยตรง ห้ามใช้เพื่อความหมายอื่น**

| Token | ค่า | ความหมาย | เกณฑ์ |
| --- | --- | --- | --- |
| `--color-status-good` | `#0F7A3D` | ดีกว่าหรือเท่ากับแผน | CPI/SPI ≥ 1.0 |
| `--color-status-good-bg` | `#E3F5EA` | พื้นหลังของสถานะดี | |
| `--color-status-watch` | `#8A5A00` | เฝ้าระวัง | 0.9 ≤ ค่า < 1.0 |
| `--color-status-watch-bg` | `#FFF4D6` | พื้นหลังของสถานะเฝ้าระวัง | |
| `--color-status-bad` | `#B3261E` | ต้องแก้ไขด่วน | ค่า < 0.9 |
| `--color-status-bad-bg` | `#FCE8E6` | พื้นหลังของสถานะแดง | |
| `--color-status-none` | `#5B6470` | คำนวณไม่ได้ แสดง "—" | BR-001-06, BR-001-14 |
| `--color-status-none-bg` | `#F1F3F5` | พื้นหลังของค่าที่คำนวณไม่ได้ | |

> **เหตุผลที่สีเฝ้าระวังเป็นน้ำตาลอำพันเข้ม ไม่ใช่เหลืองสด**: เหลืองสดบนพื้นขาวมีอัตราส่วนความต่างต่ำกว่า 3:1 และหายไปเลยกลางแดด ซึ่งเป็นสภาพใช้งานจริงของ Site Operations Display ตาม NFR-003-01

**สีกลาง**

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--color-text` | `#16191D` | ข้อความหลัก |
| `--color-text-muted` | `#5B6470` | ข้อความรอง, คำอธิบาย |
| `--color-border` | `#D5D9DE` | เส้นคั่น, ขอบการ์ด |
| `--color-surface` | `#FFFFFF` | พื้นหลังการ์ด |
| `--color-background` | `#F7F8FA` | พื้นหลังหน้า |

**โหมดกลางแดด (Outdoor)** — บังคับใช้บน Site Operations Display

| Token | ค่าปกติ | ค่ากลางแดด |
| --- | --- | --- |
| `--color-text` | `#16191D` | `#000000` |
| `--color-border` | `#D5D9DE` | `#767C85` |
| `--color-status-good` | `#0F7A3D` | `#0A5C2D` |
| `--color-status-watch` | `#8A5A00` | `#6B4600` |
| `--color-status-bad` | `#B3261E` | `#8C1D17` |

### 2.2 Typography

**ฟอนต์**

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--font-ui` | `'IBM Plex Sans Thai', 'Noto Sans Thai', sans-serif` | ข้อความทั่วไป |
| `--font-number` | `'IBM Plex Mono', ui-monospace, monospace` | ตัวเลขเงินและดัชนีทั้งหมด |

> **เหตุผลที่ตัวเลขใช้ฟอนต์ monospace แยก**: ตัวเลขในระบบนี้เป็นเงินหลักสิบล้านที่ต้องเทียบกันในตาราง ฟอนต์ที่ความกว้างตัวเลขไม่เท่ากันทำให้หลักไม่ตรงกัน อ่านผิดหลักได้ ซึ่งกระทบการตัดสินใจเรื่องงบโดยตรง

**มาตราส่วน** — ค่า `line-height` ของภาษาไทยต้องสูงกว่าภาษาอังกฤษเสมอ เพราะสระบน วรรณยุกต์ และสระล่างซ้อนกันได้ถึง 4 ชั้น

| Token | ขนาด / line-height | ใช้กับ |
| --- | --- | --- |
| `--text-display` | 32 / 44 | ตัวเลขเด่นบนการ์ด EV |
| `--text-h1` | 24 / 36 | ชื่อหน้า |
| `--text-h2` | 20 / 32 | หัวข้อส่วน |
| `--text-h3` | 18 / 28 | หัวข้อย่อย, ชื่อการ์ด |
| `--text-body` | 16 / 26 | ข้อความทั่วไป |
| `--text-body-sm` | 14 / 24 | ข้อความรอง |
| `--text-caption` | 12 / 20 | ป้ายกำกับ, timestamp |
| `--text-number-lg` | 28 / 36 | ตัวเลขหลักบนการ์ด |
| `--text-number` | 18 / 26 | ตัวเลขในตาราง |

**กฎการใช้ตัวอักษรไทย**

1. **ห้ามใช้ `text-transform: uppercase`** — ภาษาไทยไม่มีตัวพิมพ์ใหญ่ คำสั่งนี้ไม่มีผลกับไทยแต่ทำให้อังกฤษที่ปนอยู่ดูไม่เข้ากัน
2. **ห้ามใส่ `letter-spacing` บวกกับข้อความไทย** — ทำให้สระและวรรณยุกต์หลุดจากพยัญชนะที่มันเกาะอยู่
3. **ห้ามใช้ตัวเอียงกับภาษาไทย** — ฟอนต์ไทยส่วนใหญ่ไม่มีตัวเอียงจริง เบราว์เซอร์จะเอียงด้วยการบิดภาพซึ่งอ่านยาก
4. **ขนาดเล็กสุดของข้อความไทย 14px** (16px บน Site Display) — เล็กกว่านี้หัวตัวอักษรเริ่มตัน
5. **น้ำหนักที่ใช้: 400 / 600 เท่านั้น** — น้ำหนัก 500 กับ 600 ในฟอนต์ไทยแยกกันแทบไม่ออกที่ขนาดปกติ

### 2.3 Spacing

ฐาน 4px ทุกระยะต้องเป็นค่าใน token ห้ามใช้ค่าคั่นกลาง

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--space-1` | 4px | ระยะระหว่างไอคอนกับข้อความ |
| `--space-2` | 8px | ระยะภายในองค์ประกอบเล็ก |
| `--space-3` | 12px | padding ของ input |
| `--space-4` | 16px | padding ของการ์ด, ระยะระหว่างฟิลด์ |
| `--space-6` | 24px | ระยะระหว่างการ์ด |
| `--space-8` | 32px | ระยะระหว่างส่วน |
| `--space-12` | 48px | ระยะบนล่างของหน้า |
| `--space-16` | 64px | **ระยะขั้นต่ำระหว่างปุ่มบน Site Display** |

**ขนาดพื้นที่กดขั้นต่ำ**

| ส่วนงาน | ขนาดต่ำสุด | เหตุผล |
| --- | --- | --- |
| Site Operations Display | **64 × 64 px** | มือสวมถุงมือ นิ้วกว้างกว่าปกติ และผู้ใช้ยืนไม่นิ่ง (NFR-003-01) |
| Owner Portal | 48 × 48 px | ใช้บนมือถือ/แท็บเล็ต เป็นครั้งคราว |
| PM & Executive Console | 40 × 40 px | ใช้บนเดสก์ท็อปด้วยเมาส์ ตารางข้อมูลหนาแน่น |

ระยะห่างระหว่างพื้นที่กดสองอันบน Site Display ต้องไม่น้อยกว่า `--space-4` (16px) เพื่อกันการกดพลาด

### 2.4 Radius, Border, Elevation

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--radius-sm` | 4px | input, badge |
| `--radius-md` | 8px | การ์ด, ปุ่ม |
| `--radius-lg` | 12px | modal, sheet |
| `--radius-full` | 9999px | ป้ายสถานะทรงแคปซูล |
| `--border-width` | 1px | เส้นทั่วไป |
| `--border-width-thick` | 2px | ขอบเน้น, สถานะ focus |

**ความสูง (Elevation)** — มีเพียง 2 ระดับ

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--elevation-1` | `0 1px 2px rgba(0,0,0,.08)` | การ์ดที่ยกขึ้นจากพื้นหลัง |
| `--elevation-2` | `0 4px 12px rgba(0,0,0,.12)` | modal, dropdown |

> **บน Site Operations Display ห้ามใช้เงาเพื่อสื่อลำดับชั้น ให้ใช้เส้นขอบแทน** — กลางแดดจ้าเงาอ่อนมองไม่เห็นเลย ลำดับชั้นที่สื่อด้วยเงาอย่างเดียวจะหายไปทั้งหมด

### 2.5 การจัดรูปแบบตัวเลข

| ชนิด | รูปแบบ | ตัวอย่าง |
| --- | --- | --- |
| เงิน (เต็ม) | คั่นหลักพันด้วย `,` ทศนิยม 2 ตำแหน่ง ต่อท้ายด้วย "บาท" | `21,000,000.00 บาท` |
| เงิน (ย่อ) | ทศนิยม 1 ตำแหน่ง + หน่วย | `21.0 ล้านบาท` |
| ดัชนี (CPI/SPI) | ทศนิยม 2 ตำแหน่ง ไม่มีหน่วย | `1.62` |
| ส่วนต่าง (SV/CV/VAC) | มีเครื่องหมาย + หรือ − เสมอ | `+13.0 ล้านบาท` / `−12.5 ล้านบาท` |
| เปอร์เซ็นต์ | จำนวนเต็ม | `68%` |
| ค่าที่คำนวณไม่ได้ | `—` (em dash) พร้อมเหตุผล ห้ามใช้ `0`, `N/A` หรือช่องว่าง | `—` |

ตัวเลขทั้งหมดในตาราง **ชิดขวา** และใช้ `--font-number` เพื่อให้หลักตรงกัน

---

## 3. UI Components & Patterns

ทุก component ด้านล่างสืบมาจากข้อกำหนดที่ระบุไว้ ไม่มีตัวใดที่เพิ่มเข้ามาเพราะ "น่าจะมี"

### 3.1 Status Indicator

**ที่มา**: BR-001-05, FR-001-11

แสดงสถานะ CPI/SPI — ประกอบด้วย **3 ส่วนเสมอ** คือ สี + ไอคอน + ข้อความ

```
┌────────────────────────────────┐
│ ● ▲  CPI 1.62                  │
│      ได้งานเพิ่ม 62% ต่อทุกบาท   │
└────────────────────────────────┘
```

| สถานะ | สี | ไอคอน | ข้อความประกอบ |
| --- | --- | --- | --- |
| ดี | `--color-status-good` | ▲ | "ดีกว่าแผน" + คำอธิบายเป็นภาษาคน |
| เฝ้าระวัง | `--color-status-watch` | ● | "ต่ำกว่าแผนเล็กน้อย" |
| ต้องแก้ไข | `--color-status-bad` | ▼ | "ต่ำกว่าแผนมาก ต้องแก้ไขด่วน" |
| คำนวณไม่ได้ | `--color-status-none` | — | เหตุผลที่คำนวณไม่ได้ |

> **ห้ามใช้สีเป็นสัญญาณเดียว** ประชากรผู้ชายราว 8% มีภาวะตาบอดสีแดง-เขียว และงานก่อสร้างมีสัดส่วนผู้ชายสูง — ถ้าเขียวกับแดงต่างกันแค่สี ผู้ใช้กลุ่มนี้จะอ่านสถานะกลับด้านได้

### 3.2 Empty Value

**ที่มา**: BR-001-06, BR-001-14

ค่าที่คำนวณไม่ได้ต้องแสดง `—` **พร้อมเหตุผทุกครั้ง** ห้ามปล่อยขีดลอย

```
SPI  —
     ยังไม่ได้ตั้ง baseline
```

เหตุผลที่เป็นไปได้: ยังไม่ได้ตั้ง baseline / ยังไม่มีงบประมาณรวมโครงการ / ยังไม่มีการเบิกจ่าย

### 3.3 EV Card

**ที่มา**: FR-001-10, FR-001-14, FR-001-19

การ์ดใบเดียวที่แทนการ์ด "% ความคืบหน้า" กับ "% งบใช้ไป" ที่เคยแยกกัน

โครงสร้างจากบนลงล่าง:
1. **ดัชนี** CPI และ SPI พร้อม Status Indicator (§3.1)
2. **ส่วนต่าง** SV และ CV
3. **ค่าพยากรณ์** EAC, ETC, VAC
4. **ตัวเลขดิบ** BCWS / BCWP / ACWP — ขนาดเล็กลง สีจาง (FR-001-14 บังคับให้มี เพื่อให้ตรวจสอบดัชนีได้)
5. **ป้ายเวอร์ชัน baseline** ที่ใช้คำนวณ (FR-001-04)

### 3.4 Progress Stepper (0 / 50 / 100)

**ที่มา**: FR-003-10, BR-003-06

ปุ่มใหญ่ 3 ปุ่มเรียงกัน **ไม่ใช่ slider และไม่ใช่ช่องกรอกตัวเลข** — BR-003-06 รับได้เพียง 3 ค่า การใช้ slider สื่อว่ากรอกค่าอื่นได้ ซึ่งผิด

- ปุ่มละ ≥ 64px สูง เต็มความกว้างที่มี
- ปุ่มที่เลือกอยู่มีขอบหนา 2px + พื้นหลังเข้ม ไม่ใช่แค่เปลี่ยนสีอ่อนๆ
- ใต้ปุ่มมีปุ่มถ่ายรูปขนาดเท่ากัน

### 3.5 Photo Capture

**ที่มา**: FR-003-10, NFR-003-01

กล้องเป็นการกระทำหลัก ไม่ใช่ทางเลือกรอง — ปุ่มถ่ายรูปต้องใหญ่เท่าปุ่มบันทึกความคืบหน้า และรูปที่ถ่ายแล้วแสดงเป็น thumbnail ที่ลบได้ด้วยการกดค้าง (ไม่ใช่ปุ่ม × เล็กๆ ที่กดพลาดง่ายด้วยถุงมือ)

### 3.6 Offline Indicator & Sync Queue

**ที่มา**: FR-003-12, NFR-003-02

แถบสถานะการเชื่อมต่อ**ติดอยู่ตลอดเวลา**บน Site Display ไม่ใช่ toast ที่หายไป

| สถานะ | รูปแบบ |
| --- | --- |
| ออนไลน์ ซิงก์ครบ | แถบบางสีเทา "ซิงก์ล่าสุด 10:42" |
| ออฟไลน์ มีคิวค้าง | แถบสีอำพัน "ออฟไลน์ — มี 7 รายการรอส่ง" |
| กำลังซิงก์ | แถบสีน้ำเงิน + ตัวนับ |
| ซิงก์ล้มเหลว | แถบสีแดง + ปุ่ม "ดูรายการที่ค้าง" |

> ข้อมูลที่บันทึกออฟไลน์ต้องไม่หายเมื่อปิดแอปหรือแบตหมด (NFR-003-02) — UI ต้องยืนยันการบันทึกลงเครื่องทันที ไม่ใช่รอผลจากเซิร์ฟเวอร์

### 3.7 Conflict Resolution

**ที่มา**: FR-003-13, BR-003-10

เมื่อข้อมูลชนกันตอน sync **แสดงทั้งสองค่าคู่กันให้คนเลือก ห้ามเลือกให้เอง**

- ฝั่งซ้าย: ค่าบนเครื่อง (ใคร บันทึกเมื่อไร)
- ฝั่งขวา: ค่าบนเซิร์ฟเวอร์ (ใคร บันทึกเมื่อไร)
- ปุ่มเลือกขนาดเท่ากันทั้งสองฝั่ง — ห้ามทำให้ฝั่งใดดูเป็นตัวเลือกเริ่มต้น

### 3.8 Stale Drawing Warning

**ที่มา**: FR-003-14, BR-003-04

เมื่อเปิดแบบก่อสร้างที่ cache ไว้ขณะออฟไลน์ ต้องมีแถบเตือนค้างอยู่บนแบบตลอดเวลาที่เปิด ไม่ใช่ dialog ที่ปิดแล้วหาย

```
⚠ แบบนี้อาจไม่ใช่ฉบับล่าสุด — ซิงก์ครั้งสุดท้าย 15 ส.ค. 16:20
```

### 3.9 Issue Card

**ที่มา**: FR-002-08

Issue ที่ระบบสร้างจากกฎ EVM ต้องแยกจาก Issue ที่คนแจ้งได้ด้วยตาเปล่า — ใช้ป้ายกำกับ "สร้างโดยระบบ" + แถบสีซ้ายมือ ไม่ใช่แค่ไอคอนเล็กๆ

การ์ดต้องแสดง: กฎที่เข้าเงื่อนไข, ค่าที่วัดได้, งวด, เวอร์ชัน baseline, ลิงก์ไป snapshot ต้นทาง (FR-002-06)

### 3.10 Approval Action Pair

**ที่มา**: FR-003-06, BR-003-07

คู่ปุ่ม "อนุมัติ / ตีกลับ" ที่ใช้กับงวดงานและคำขอ re-baseline

- **ปุ่มอนุมัติ**: สีหลัก น้ำหนักเต็ม
- **ปุ่มตีกลับ**: ปุ่มขอบ ไม่ใช่สีแดง — การตีกลับเป็นส่วนหนึ่งของกระบวนการปกติ ไม่ใช่การทำลาย
- **ทั้งสองปุ่มบังคับให้ระบุเหตุผลก่อนยืนยัน** และเหตุผลนั้นถูกเก็บถาวร
- ปุ่มยืนยันขั้นสุดท้ายต้องสรุปสิ่งที่กำลังจะเกิดขึ้นเป็นภาษาคน ไม่ใช่ "ยืนยัน?"

### 3.11 Baseline Version Badge

**ที่มา**: FR-001-04, BR-001-03

ป้ายเล็กบอกว่าดัชนีที่เห็นคำนวณจาก baseline เวอร์ชันใด กดแล้วเปิดประวัติทุกเวอร์ชันพร้อมเหตุผลของการเปลี่ยน

### 3.12 EV Chart

**ที่มา**: FR-001-12, FR-001-16, BR-001-10

กราฟเส้นเทียบ BCWS / ACWP / BCWP

- **เส้นปัจจุบันทึบ เส้น baseline เก่าจาง** แสดงย้อนหลังได้ไม่เกิน 2 เวอร์ชัน
- เส้นทั้ง 3 ต้องแยกกันได้โดยไม่พึ่งสีอย่างเดียว — ใช้รูปแบบเส้น (ทึบ/ประ/จุด) ประกอบ
- แกน Y เป็นเงิน ใช้หน่วยย่อ ("ล้านบาท") ไม่ใช่ตัวเลขเต็มที่ยาวจนอ่านไม่ทัน

### 3.13 Data Table

- หัวตารางติดด้านบนเมื่อเลื่อน
- คอลัมน์ตัวเลขชิดขวา ใช้ `--font-number`
- แถวสลับสีอ่อนๆ เฉพาะตารางที่เกิน 8 แถว
- บนหน้าจอแคบ ตารางกลายเป็นการ์ดเรียงกัน ไม่ใช่เลื่อนแนวนอน

### 3.14 Status Badge (draft / approved)

ป้ายบอกสถานะเอกสารหรือรายการ ใช้ทรงแคปซูล `--radius-full`

| สถานะ | รูปแบบ |
| --- | --- |
| `(draft)` | ขอบประ พื้นโปร่ง สีเทา — สื่อว่ายังเปลี่ยนได้ |
| `approved` | พื้นทึบสีหลัก |
| `ยกเลิก` | ข้อความขีดฆ่า สีจาง |

---

## 4. UX Guidelines & Rules

กฎเหล่านี้มีลำดับ — เมื่อขัดกัน ข้อที่อยู่บนกว่าชนะ

### กฎที่ 1 — ตัวเลขต้องอ่านรู้เรื่องโดยไม่ต้องรู้ศัพท์ EVM

**ที่มา**: NFR-002-03

ทุกดัชนีที่แสดงต้องมีคำอธิบายภาษาไทยกำกับ ผู้บริหารที่ไม่เคยเรียน EVM ต้องเข้าใจได้ทันที ศัพท์อย่าง CPI, SPI, BCWS ปรากฏได้ในฐานะ*ป้ายกำกับ* ไม่ใช่ในฐานะ*คำอธิบาย*

### กฎที่ 2 — ห้ามให้ผู้ใช้เดาว่าทำไมไม่มีข้อมูล

**ที่มา**: BR-001-06, BR-001-14

`—` ต้องมาพร้อมเหตุผลเสมอ และเหตุผลต้องบอกว่าต้องทำอะไรจึงจะมีข้อมูล ไม่ใช่แค่บอกว่าไม่มี

### กฎที่ 3 — สีไม่ใช่สัญญาณเดียว

ทุกความหมายที่สื่อด้วยสี ต้องมีไอคอนหรือข้อความประกอบเสมอ (§3.1)

### กฎที่ 4 — สิ่งที่บทบาทนั้นไม่มีสิทธิ์เห็น ต้องไม่ถูกส่งมาที่หน้าจอ

**ที่มา**: FR-003-07, FR-003-09, BR-003-01

ห้ามซ่อนด้วย CSS ห้ามแสดงเป็นช่องล็อก — ข้อมูลที่บทบาทนั้นเห็นไม่ได้ต้องไม่ออกจากชั้นข้อมูลตั้งแต่แรก ใช้หลัก **default-deny**: สิ่งที่ไม่อยู่ในรายการอนุญาตถือว่าปฏิเสธ

### กฎที่ 5 — การกระทำที่ย้อนกลับยาก ต้องยืนยันพร้อมเหตุผล

การล็อก baseline, การตีกลับงวดงาน, การยืนยัน Change Order, การปิดโครงการ — ทั้งหมดต้องมีขั้นยืนยันที่สรุปผลที่จะเกิดขึ้น และเก็บเหตุผลถาวร

### กฎที่ 6 — บน Site Display หนึ่งหน้าจอมีการกระทำหลักได้อย่างเดียว

**ที่มา**: NFR-003-01

ผู้ใช้ยืนอยู่หน้างาน มือข้างหนึ่งอาจถืออย่างอื่น หน้าจอที่มีปุ่มหลัก 3 ปุ่มเท่ากันคือหน้าจอที่กดผิด

### กฎที่ 7 — ถ่ายรูปแทนการพิมพ์ทุกที่ที่ทำได้

**ที่มา**: NFR-003-01

ถ้าข้อมูลบันทึกด้วยรูปได้ ต้องให้ถ่ายรูปเป็นทางหลัก และให้พิมพ์เป็นทางเลือกรอง

### กฎที่ 8 — สถานะออฟไลน์ต้องมองเห็นตลอดเวลา ไม่ใช่แจ้งเตือนที่หายไป

**ที่มา**: FR-003-12, NFR-003-02

และการบันทึกต้องยืนยัน "เก็บลงเครื่องแล้ว" ทันที ไม่ใช่รอผลจากเซิร์ฟเวอร์

### กฎที่ 9 — ข้อมูลที่ชนกันให้คนตัดสิน ห้ามระบบเลือกเอง

**ที่มา**: BR-003-10

### กฎที่ 10 — ไม่มี animation ที่หน่วงการเห็นข้อมูล

Transition สูงสุด 150ms และห้ามใช้กับการโหลดข้อมูล ใช้ skeleton แทน spinner สำหรับพื้นที่ที่รู้ขนาดล่วงหน้า

### กฎที่ 11 — ภาษาไทยเท่านั้นใน UI

ยกเว้นศัพท์เทคนิคที่เป็นป้ายกำกับของค่ามาตรฐาน (CPI, SPI, EAC, BCWS) ซึ่งต้องมีคำอธิบายไทยกำกับตามกฎที่ 1

### 4.12 การเข้าถึง (Accessibility)

| หัวข้อ | เกณฑ์ |
| --- | --- |
| ความต่างของสีข้อความ | ≥ 4.5:1 (ปกติ) / ≥ 7:1 (โหมดกลางแดด) |
| ความต่างขององค์ประกอบ UI | ≥ 3:1 |
| สถานะ focus | ขอบ 2px สีหลัก มองเห็นชัดทุกองค์ประกอบที่กดได้ |
| การใช้แป้นพิมพ์ | ทุกการกระทำบน PM Console ทำด้วยแป้นพิมพ์ได้ |
| ป้ายกำกับ | ทุก input มี label จริง ไม่ใช้ placeholder แทน label |

---

## 5. ส่วนงานทั้ง 3 และความหนาแน่นที่ต่างกัน

**ที่มา**: FR-003-01

| | Owner Portal | Site Operations Display | PM & Executive Console |
| --- | --- | --- | --- |
| อุปกรณ์หลัก | มือถือ / แท็บเล็ต | มือถือกลางแจ้ง | เดสก์ท็อป |
| ขนาดตัวอักษรฐาน | 16px | **18px** | 16px |
| พื้นที่กดต่ำสุด | 48px | **64px** | 40px |
| โหมดกลางแดด | ไม่บังคับ | **บังคับ** | ไม่ใช้ |
| ลำดับชั้นสื่อด้วย | เงา + เส้น | **เส้นเท่านั้น** | เงา + เส้น |
| ความหนาแน่นข้อมูล | ต่ำ — สรุปเป็นหลัก | ต่ำมาก — งานเดียวต่อจอ | สูง — ตารางและกราฟ |
| การกระทำหลักต่อจอ | 1–2 | **1** | ไม่จำกัด |

---

## 6. สิ่งที่ยังต้องตัดสิน

รายการต่อไปนี้ผมเลือกค่าตั้งต้นไว้ให้แล้วเพื่อให้เริ่มงานได้ทันที แต่เป็นการตัดสินใจของเจ้าของผลิตภัณฑ์ — ถ้าไม่ตรงกับที่ต้องการ บอกได้

| # | เรื่อง | ค่าที่ใช้อยู่ | ทางเลือกอื่น |
| --- | --- | --- | --- |
| 1 | สีหลักของแบรนด์ | `#1B3A5C` น้ำเงินเข้มอมเทา | เขียวเข้ม (สื่อความปลอดภัย แต่ชนกับสีสถานะดี) / ส้มอิฐ (สื่องานก่อสร้าง แต่ใกล้สีเตือน) |
| 2 | ฟอนต์ | IBM Plex Sans Thai + IBM Plex Mono | Sarabun (มาตรฐานราชการไทย เหมาะถ้าต้องส่งเอกสารราชการ) / Noto Sans Thai (ครอบคลุมอักขระกว้างที่สุด) |
| 3 | ชื่อผลิตภัณฑ์ที่แสดงบน UI | BuildTrack | ชื่อไทย / ชื่อผสม |
| 4 | โหมดมืด | ยังไม่รองรับ | รองรับเฉพาะ PM Console / รองรับทุกส่วนงาน |

---

## 7. เอกสารอ้างอิง

- ข้อกำหนดที่เอกสารนี้อ้างถึง: [[docs/01-requirements/01-spec/index|01-spec]]
- รายการงานที่เกี่ยวข้อง: [[docs/01-requirements/backlog|Product Backlog]]
- ต้นแบบหน้าจอ (เฟสถัดไป): [[docs/02-design/01-prototypes/index|01-prototypes]]
